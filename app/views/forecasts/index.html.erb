<div class="main-content">
  <div class="wrap">
    <!--<%= render partial: "slide/slide"%>
    <div class="center">
      <div class="main-content-btn pt-2">
        <% if user_signed_in? %>
          <%= link_to "投稿する",new_forecast_path,class: "send_btn"%>
        <% end %>
        <% unless current_page?(root_path)%>
          <%= link_to "トップページへ戻る",root_path,class: "return-top"%>
        <%end%>
      </div>
      <div class="text-format mt-5 mb-4 text-warning">
        試合予想をしてみましょう！
      </div>
      <div class="text-format mt-5 mb-4 text-warning">
        大会別
      </div>
      <div class="title pb-5 mt-5">
        <% @category_parent_array.each do |tournament|%>
          <%= link_to "/tournaments/#{tournament.id}/watch_fcs",class: "title-child text-white" do %>
            <i class="fa fa-baseball-ball text-white"></i><%= tournament.name %>
          <% end %>
        <% end %>
      </div>
      <%= form_with(url: search_forecasts_path, local: true, method: :get, class: "search mt-5 pb-5") do |form| %>
        <%= form.text_field :keyword, placeholder: "記事を検索する", class: "search-input" %>
        <%= form.submit "検索", class: "search-btn" %>
      <% end %>
    </div>
  </div>
  <%# <div class="forecast-main"> %>
    <%# <%= render partial: "forecast_show"%> %>
  <%# </div> %>
</div>-->
<%= javascript_pack_tag 'main' %>